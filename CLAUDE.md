# CLAUDE.md

## 修 Bug 规范

- 修改任何 class/function 签名前，先 grep 项目里所有调用点
- 修完一个调用点后，检查同一函数/方法的所有其他调用点是否需要同步修改
- 关注"组装"和"使用"两层：构造函数参数改了，调用方的传参也要检查
- 不要只修报错那一行，顺着调用链上下游都检查一遍

## 写测试规范

- 测试必须覆盖**实际代码路径**，不是理想调用方式
- 补测试时，先看实际代码怎么调用的，再写测试断言
- CLI 命令要有端到端冒烟测试，走真实的命令入口

## 改完代码必须验证

改完代码跑一次质量门禁：
```bash
bash scripts/check.sh
```
这个脚本按顺序跑四层检查：pyright 类型检查 → ruff lint → pytest → 端到端冒烟。全过才算改完。

不要手动挑着跑，直接跑脚本，它会告诉你哪层没过。
